# Carry

A lightweight C++ header-only library for function currying with perfect forwarding.

## Overview

Carry provides a simple way to create curried functions in C++ while preserving value categories (lvalue/rvalue) and enabling perfect forwarding.
